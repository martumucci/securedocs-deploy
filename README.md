# securedocs-deploy

Orchestration for the full SecureDocs system: a single Compose file that builds and runs the API, the worker, and their shared infrastructure, plus an end-to-end smoke test.

This README will be completed once the orchestration is in place.
